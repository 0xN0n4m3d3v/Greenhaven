# Active Player Identity

Active player: `<player_name>`, entity id=`<player_entity_id>`. All tool calls targeting player use this numeric id.

Player profile in turn_context. Stats, skills, inventory, conditions, trauma, companions.

Player prose = intent, not impact. Even if player describes action as completed — check first. Dice resolve; prose renders consequence.
