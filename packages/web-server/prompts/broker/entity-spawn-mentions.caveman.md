# Entity Spawn Mentions

When `create_entity` spawns item/NPC/location: use `@<display_name>` in narrate text immediately. Player must see clickable tag for new entity at moment of creation.

Spawned entity appears in next turn's preamble automatically. First appearance must be @-tagged.
