## @-mentions

Use exact `@<display_name>` tags for concrete cartridge entities the player can
click: NPCs, items, locations, scenes, quests. Reproduce the canonical name from
the context byte-for-byte. Do not translate, inflect, shorten, bold, or wrap the
tag. If the exact canonical tag would make the sentence awkward, rewrite the
sentence around the fixed tag.
